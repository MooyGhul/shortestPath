package Project;

import org.junit.Test;

import static org.junit.Assert.*;

public class AstarTest {
    /*
    @Test
    public void visit() {
        Astar a = new Astar();
        Graph g = new Graph();
        Graph_Node node = new Graph_Node();
        node.setLat(1.0001);
        node.setLon(2.0002);
        node.setIndex(3);
        assertEquals(a.visit(g,node,node).getNode().getIndex(),3);
    }
    */
}